<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.mvc.menu.dao.MN001Dao">

    <select id="getHorizen" resultType="MN001Dto">
        select  menu_code   as  menuCode,
                parent_code as  parentCode,
                sort_seq    as  sortSeq,
                menu_type   as  menuType,
                menu_nm     as  menuNm
        from tree_menu
        <where>
            menu_type = 'H'
            <if test="auth != null and auth != ''">
                and menu_permit &gt;= #{auth}
            </if>
        </where>
        order by menu_code, sort_seq
    </select>

    <select id="getTopMenu" resultType="MN001Dto">
        select  menu_code   as  menuCode,
                parent_code as  parentCode,
                sort_seq    as  sortSeq,
                menu_type   as  menuType,
                menu_nm     as  menuNm,
                menu_icon   as  menuIcon,
                menu_url    as  menuUrl
        from tree_menu
        <where>
            menu_type = 'F'
            <if test="auth != null and auth != ''">
                and menu_permit &gt;= #{auth}
            </if>
        </where>
        order by menu_code, sort_seq
    </select>

    <select id="getLeafMenu" resultType="MN001Dto">
        select  menu_code   as  menuCode,
                parent_code as  parentCode,
                sort_seq    as  sortSeq,
                menu_type   as  menuType,
                menu_nm     as  menuNm,
                menu_icon   as  menuIcon,
                menu_url    as  menuUrl
        from tree_menu
        <where>
            menu_type = 'L'
            <if test="auth != null and auth != ''">
                and menu_permit &gt;= #{auth}
            </if>
        </where>
        order by menu_code, sort_seq
    </select>
</mapper>